# =============================================================
# requirements.txt — Telecom RCA Analyzer
# Install with: pip install -r requirements.txt
#
# Uses >= pins instead of == so pip can find pre-built wheels
# for Python 3.14 on Windows (avoids source compilation errors).
# =============================================================

# ── PCAP parsing ──────────────────────────────────────────────
# ── Data processing ───────────────────────────────────────────
pandas>=2.1.0
numpy>=1.26.0

# ── Machine learning ──────────────────────────────────────────
xgboost>=2.0.0
scikit-learn>=1.3.0

# ── Explainability ────────────────────────────────────────────
# SHAP for per-prediction feature attribution
shap>=0.43.0

# ── Model persistence ─────────────────────────────────────────
joblib>=1.3.0

# ── Web backend ───────────────────────────────────────────────
# Flask serves the REST API consumed by telecom_rca_gui.html
flask>=3.0.0
flask-cors>=4.0.0
waitress>=3.0.0

# ── Configuration ─────────────────────────────────────────────
# Used by src/config.py to load config.yaml
pyyaml>=6.0

# ── Visualisation ─────────────────────────────────────────────
# Used for optional SHAP summary plots (not required for GUI)
matplotlib>=3.7.0

# ── CLI / logging / display ───────────────────────────────────
rich>=13.0.0
click>=8.1.0
loguru>=0.7.0
tabulate>=0.9.0
pytest>=8.0.0

# =============================================================
# NOT included (intentionally removed vs previous version):
#   scapy  — was unused, caused source-build failures on Py 3.14
# =============================================================
